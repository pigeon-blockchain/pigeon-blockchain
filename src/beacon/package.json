{
    "name": "pigeon-beacon",
    "scripts": {
        "build": "buildah unshare ./build-flock.sh",
        "clean": "tsc --build --clean",
        "distclean": "rm -fr *.js node_modules store *.log package-lock.json"
    }
}

